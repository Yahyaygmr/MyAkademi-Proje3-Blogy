@model List<WriterMessage>

<a class="nav-link count-indicator dropdown-toggle" id="messageDropdown" href="#" data-toggle="dropdown">
    <i class="mdi mdi-email-outline"></i>
    @if (ViewBag.isReadCount>0)
    {
        <span class="count count-varient2">@ViewBag.isReadCount</span>
    }
    
</a>
<div class="dropdown-menu navbar-dropdown navbar-dropdown-large preview-list" aria-labelledby="messageDropdown">
    <h6 class="p-3 mb-0">Mesajlar</h6>
    @foreach (var item in Model)
    {
        <a class="dropdown-item preview-item">
            <div class="preview-item-content flex-grow">
                <span class="badge badge-pill badge-success">@item.AppUser.Name @item.AppUser.Surname</span>
                <p class="text-small text-muted ellipsis mb-0"> @item.Subject </p>
            </div>
            <p class="text-small text-muted align-self-start"> @item.SendDate.ToString("HH:mm") </p>
        </a>
    }
  
    <h6 class="p-3 mb-0">Tüm Mesajları Gör</h6>
</div>